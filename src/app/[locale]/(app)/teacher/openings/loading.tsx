import { PageSkeleton } from "@/components/shared/page-skeleton";

/** Loading UI for the teacher openings (apply-to-teach) pages. */
export default function TeacherOpeningsLoading() {
  return <PageSkeleton />;
}
